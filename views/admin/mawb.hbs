<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern MAWB Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #0ea5e9;
            --secondary-color: #0f172a;
            --accent-color: #38bdf8;
            --border-color: #e2e8f0;
            --hover-color: #0284c7;
            --bg-color: #f8fafc;
            --shadow-color: rgba(0, 0, 0, 0.04);
            --text-color: #334155;
            --section-bg: #ffffff;
            
            --primary-gradient: linear-gradient(135deg, #4361ee 0%, #4895ef 100%);
            --glass-bg: rgba(255, 255, 255, 0.95);
            --glass-shadow: 0 8px 32px rgba(31, 38, 135, 0.15);
            --card-border: 1px solid rgba(255, 255, 255, 0.18);
            --neon-glow: 0 0 15px rgba(67, 97, 238, 0.3);
            --sidebar-width: 280px;
            --sidebar-bg: rgba(15, 23, 42, 0.95);
            --sidebar-hover: rgba(30, 41, 59, 0.8);
            --neon-accent: #0ea5e9;
            --glass-effect: blur(10px);
        }

        /* Sidebar Base Styles */
        .sidebar {
            width: var(--sidebar-width);
            background: var(--sidebar-bg);
            backdrop-filter: var(--glass-effect);
            -webkit-backdrop-filter: var(--glass-effect);
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            position: fixed;
            height: 100vh;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            overflow-y: auto;
            scrollbar-width: none;
            z-index: 1000;
        }

        .sidebar::-webkit-scrollbar {
            display: none;
        }

        /* Logo Section */
        .sidebar .logo {
            padding: 2rem 1.5rem;
            background: linear-gradient(
                to bottom,
                rgba(15, 23, 42, 0.98),
                rgba(15, 23, 42, 0.95)
            );
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .sidebar .logo h4 {
            font-size: 1.25rem;
            font-weight: 600;
            background: linear-gradient(120deg, #fff, var(--neon-accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: 0.5px;
        }

        /* Navigation Links */
        .sidebar .nav-link {
            color: rgba(255, 255, 255, 0.7);
            padding: 1rem 1.5rem;
            margin: 0.3rem 0.8rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            position: relative;
            text-decoration: none;
        }

        .sidebar .nav-link:hover {
            background: var(--sidebar-hover);
            color: var(--neon-accent);
            box-shadow: var(--neon-glow);
        }

        .sidebar .nav-link i {
            margin-right: 1rem;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .sidebar .nav-link.active-nav-link {
            background: rgba(14, 165, 233, 0.15);
            color: var(--neon-accent);
            box-shadow: var(--neon-glow);
        }

        .sidebar .nav-link.active-nav-link::before {
            content: '';
            position: absolute;
            left: -0.8rem;
            top: 50%;
            transform: translateY(-50%);
            width: 4px;
            height: 20px;
            background: var(--neon-accent);
            border-radius: 0 4px 4px 0;
            box-shadow: var(--neon-glow);
        }

        .nav.flex-column {
            padding: 1rem 0;
        }
        
        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            background: linear-gradient(135deg, #f6f7ff 0%, #eef1ff 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        .main-container {
            min-height: 100vh;
            margin-left: var(--sidebar-width);
            padding: 2rem;
            background: linear-gradient(135deg, #f6f7ff 0%, #eef1ff 100%);
            transition: margin-left 0.4s ease;
        }

        .header {
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            padding: 3rem 2rem;
            border-radius: 20px;
            margin-bottom: 2rem;
            box-shadow: 0 15px 30px rgba(14, 165, 233, 0.15);
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 80%);
            transform: rotate(-45deg);
            pointer-events: none;
        }

        .header h1 {
            font-weight: 700;
            font-size: 2.75rem;
            margin-bottom: 1rem;
            color: white;
            position: relative;
            z-index: 1;
        }

        .header p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1rem;
            position: relative;
            z-index: 1;
        }

        /* Improved Section Styling */
        .form-section {
            background: var(--glass-bg);
            border-radius: 20px;
            box-shadow: var(--glass-shadow);
            padding: 2.5rem;
            margin-bottom: 2.5rem;
            transition: all 0.3s ease;
            border: var(--card-border);
            position: relative;
            overflow: hidden;
        }

        .form-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: var(--primary-gradient);
            border-radius: 20px 20px 0 0;
        }

        .form-section:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        /* Enhanced Section Title */
        .section-title {
            font-size: 1.6rem;
            font-weight: 600;
            color: var(--secondary-color);
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid rgba(226, 232, 240, 0.6);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .section-title i {
            color: var(--primary-color);
            font-size: 1.4rem;
            background: rgba(14, 165, 233, 0.1);
            padding: 0.5rem;
            border-radius: 10px;
        }

        /* Improved Form Controls */
        .form-control, .form-select {
            border: 1.5px solid var(--border-color);
            padding: 0.9rem 1rem;
            border-radius: 12px;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            background-color: #ffffff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.02);
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.15);
            transform: translateY(-2px);
        }

        /* Form Label Styling */
        .form-label {
            font-weight: 500;
            color: var(--secondary-color);
            margin-bottom: 0.75rem;
            font-size: 1rem;
            position: relative;
            display: block;
        }

        .required-field::after {
            content: "*";
            color: #ef4444;
            margin-left: 4px;
            font-size: 1.1rem;
        }

        /* Enhanced Search Input */
        .search-input {
            background-color: rgba(241, 245, 249, 0.6);
            border: 1.5px solid var(--border-color);
            padding: 0.9rem 1rem;
            border-radius: 12px;
            margin-bottom: 1.2rem;
            width: 100%;
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.02);
        }

        .search-input:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.15);
        }

        /* Cargo Item Styling */
        .cargo-item {
            background: rgba(255, 255, 255, 0.5);
            border-radius: 15px;
            padding: 1.5rem;
            margin-top: 1.5rem;
            border: 1px solid rgba(226, 232, 240, 0.8);
            position: relative;
            transition: all 0.3s ease;
        }

        .cargo-item:hover {
            background: rgba(255, 255, 255, 0.8);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
        }

        .cargo-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--secondary-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
        }

        .cargo-title::before {
            content: '';
            display: inline-block;
            width: 12px;
            height: 12px;
            background: var(--primary-color);
            border-radius: 50%;
            margin-right: 0.75rem;
        }

        /* Button Styling */
        .btn-primary {
            background: var(--primary-gradient);
            border: none;
            padding: 0.8rem 1.5rem;
            font-weight: 500;
            border-radius: 12px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.15);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--neon-glow);
        }

        .btn-danger {
            background: linear-gradient(135deg, #f43f5e 0%, #e11d48 100%);
            border: none;
            color: white;
            padding: 0.8rem 1.5rem;
            font-weight: 500;
            border-radius: 12px;
            transition: all 0.3s ease;
        }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(225, 29, 72, 0.2);
        }

        /* Improve submit button */
        .btn-submit {
            background: var(--primary-gradient);
            border: none;
            padding: 1.25rem 3.5rem;
            font-weight: 600;
            border-radius: 15px;
            transition: all 0.3s ease;
            font-size: 1.1rem;
            letter-spacing: 0.5px;
            box-shadow: var(--glass-shadow);
            position: relative;
            overflow: hidden;
        }

        .btn-submit::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: var(--neon-glow), 0 10px 25px rgba(14, 165, 233, 0.3);
        }

        .btn-submit:hover::before {
            left: 100%;
        }

        /* HAWB Button styling */
        #hawbButtonContainer {
            margin-top: 2rem;
            text-align: center;
            transition: all 0.5s ease;
        }

        #createHawbBtn {
            background: var(--secondary-color);
            color: white;
            border: none;
            padding: 1rem 2.5rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
        }

        #createHawbBtn::before {
            content: '\F4DE';
            font-family: 'bootstrap-icons';
            font-size: 1.2rem;
        }

        #createHawbBtn:hover {
            background: #1e293b;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(15, 23, 42, 0.2);
        }

        /* Form check improvements */
        .form-check {
            margin-top: 1rem;
            display: flex;
            align-items: center;
        }

        .form-check-input {
            width: 1.2em;
            height: 1.2em;
            margin-top: 0;
            border: 1.5px solid var(--border-color);
            border-radius: 0.25em;
            transition: all 0.2s ease;
        }

        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            box-shadow: 0 0 0 2px rgba(14, 165, 233, 0.2);
        }

        .form-check-label {
            margin-left: 0.5rem;
            font-size: 0.95rem;
            color: var(--text-color);
        }

        /* Field group spacing */
        .row.g-4 > * {
            padding-top: 0.75rem;
            padding-bottom: 0.75rem;
        }

        /* Form field tooltip */
        .info-tooltip {
            color: var(--primary-color);
            cursor: pointer;
            margin-left: 0.5rem;
            transition: all 0.2s ease;
        }

        .info-tooltip:hover {
            color: var(--hover-color);
            transform: scale(1.1);
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .main-container {
                margin-left: 0;
                padding: 1rem;
            }
            
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.show {
                transform: translateX(0);
            }
        }

        @media (max-width: 768px) {
            .header {
                padding: 2rem 1.5rem;
            }

            .header h1 {
                font-size: 2rem;
            }

            .form-section {
                padding: 1.5rem;
            }

            .btn-submit {
                width: 100%;
                padding: 1rem;
            }

            .section-title {
                font-size: 1.4rem;
            }
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="logo">
                <h4 class="mb-0">E Gate Shipping</h4>
            </div>
            <div class="nav flex-column">
                <a href="/admin/track" class="nav-link">
                    <i class="bi bi-truck"></i> 
                    Tracking
                </a>

                <a href="/admin/createAwb" class="nav-link">
                    <i class="bi bi-files"></i>
                    Create AWB
                </a>
                 
                <a href="/admin/mawb" class="nav-link active-nav-link">
                    <i class="bi bi-file-earmark-break"></i> 
                    Master AWB
                </a>

                <a href="/admin/fetchShipment" class="nav-link">
                    <i class="bi bi-pencil-square"></i>
                    Update AWB
                </a>
                <a href="/admin/getShipment" class="nav-link">
                    <i class="bi bi-boxes"></i>
                    All Shipments
                </a>
                <a href="/admin/airlines" class="nav-link">
                    <i class="bi bi-cloud"></i>
                    Airlines
                </a>
                <a href="/admin/profile" class="nav-link">
                    <i class="bi bi-gear"></i>
                    Admin Profile
                </a>
            </div>
        </nav>
        
        <div class="main-container">
            <div class="container">
                <div class="header text-center">
                    <h1>Master Air Waybill</h1>
                    <p class="mb-0">Digital Air Cargo Management System</p>
                </div>

                <form id="mawbForm" action="/admin/generate-mawb" method="POST">
                    <!-- AWB Details -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-file-earmark-text"></i>
                            AWB Details
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Select Airline</label>
                                <select class="form-select" id="airlineSelect" name="airline">
                                    <option value="" selected disabled>Select Airline</option>
                                    {{#each airlines}}
                                        <option value="{{prefix}}">{{airlineName}}</option>
                                    {{/each}}
                                </select>
                            </div>

                            <div class="col-md-4" id="awbNumberContainer" style="display:none;">
                                <label class="form-label">AWB Number</label>
                                <select class="form-select" id="awbNumberSelect" name="awbNumber">
                                    <option value="" selected disabled>Select AWB Number</option>
                                    <!-- AWB numbers will be dynamically populated -->
                                </select>
                            </div>

                            <div class="col-md-4">
                                <label class="form-label">Agents IATA Code</label>
                                <input type="text" class="form-control" name="iataCode" placeholder="Enter IATA code">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Issued By</label>
                                <input type="text" class="form-control" name="carrierCode" placeholder="Issuing carrier code">
                            </div>
                        </div>
                    </div>

                    <!-- Shipper & Consignee -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-people-fill"></i>
                            Shipper & Consignee Information
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <input type="text" class="search-input" placeholder="Search shipper in address book">
                                </div>
                                <label class="form-label required-field">Shipper Details</label>
                                <textarea class="form-control" name="shipperName" required 
                                        placeholder="Enter complete shipper information"></textarea>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="saveShipperAddress">
                                    <label class="form-check-label" for="saveShipperAddress">
                                        Save to address book
                                    </label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <input type="text" class="search-input" placeholder="Search consignee in address book">
                                </div>
                                <label class="form-label required-field">Consignee Details</label>
                                <textarea class="form-control" name="consigneeName" required 
                                        placeholder="Enter complete consignee information"></textarea>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="saveConsigneeAddress">
                                    <label class="form-check-label" for="saveConsigneeAddress">
                                        Save to address book
                                    </label>
                                </div>
                            </div>
                            <div class="col-md-12 mt-3">
                                <label class="form-label">Accounting Info</label>
                                <input type="text" class="form-control" name="frieght" placeholder="Enter accounting information">
                            </div>
                        </div>
                    </div>

                    <!-- Routing Information -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-geo-alt-fill"></i>
                            Routing Information
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-3">
                                <label class="form-label">Departure</label>
                                <input type="text" class="form-control" name="departure" placeholder="Airport code">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Destination</label>
                                <input type="text" class="form-control" name="destination" placeholder="Airport code">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Routing TO1</label>
                                <input type="text" class="form-control" name="routingTO1" placeholder="Via point 1">
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Routing TO2</label>
                                <input type="text" class="form-control" name="ICN" placeholder="Via point 2">
                            </div>
                        </div>
                    </div>

                    <!-- Cargo Details -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-box-seam"></i>
                            Cargo Details
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Gross Weight</label>
                                <input type="text" class="form-control" name="grossWeight" placeholder="Weight in kg">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Nature and Quantity of Goods</label>
                                <input type="text" class="form-control" name="consolidated" placeholder="Goods description">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Dimensions</label>
                                <input type="text" class="form-control" name="dimensions" placeholder="LxWxH">
                            </div>
                            <div class="col-12 mt-3">
                                <label class="form-label">Handling Information</label>
                                <textarea class="form-control" name="handlingInformation" rows="3" placeholder="Special handling instructions"></textarea>
                            </div>
                            <div class="col-md-3 mt-3">
                                <label class="form-label">Prepaid</label>
                                <input type="text" class="form-control" name="prepaid" placeholder="Prepaid amount">
                            </div>
                            <div class="col-md-3 mt-3">
                                <label class="form-label">Rate Charge</label>
                                <input type="text" class="form-control" name="RATECHARGE" placeholder="Rate amount">
                            </div>
                            <div class="col-md-3 mt-3">
                                <label class="form-label">Total Charges Carrier</label>
                                <input type="text" class="form-control" name="totalChargesCarrier" placeholder="Total charges">
                            </div>
                            <div class="col-md-3 mt-3">
                                <label class="form-label">Total QTY</label>
                                <input type="text" class="form-control" name="totalQTY" placeholder="Total quantity">
                            </div>
                            <div class="col-md-3 mt-3">
                                <label class="form-label">QTY</label>
                                <input type="text" class="form-control" name="QTY" placeholder="Quantity">
                            </div>
                        </div>
                    </div>

                    <!-- Additional Information -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-info-circle"></i>
                            Additional Information
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">PP</label>
                                <input type="text" class="form-control" name="PP" placeholder="PP value">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">PPD</label>
                                <input type="text" class="form-control" name="COLUMN1" placeholder="PPD value">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">COLL</label>
                                <input type="text" class="form-control" name="COLUMN2" placeholder="COLL value">
                            </div>
                            <div class="col-md-6 mt-3">
                                <label class="form-label">By</label>
                                <input type="text" class="form-control" name="By" placeholder="By information">
                            </div>
                            <div class="col-md-6 mt-3">
                                <label class="form-label">By First Carrier</label>
                                <input type="text" class="form-control" name="firstcarrier" placeholder="First carrier information">
                            </div>
                        </div>
                    </div>

                    <!-- Signature Section -->
                    <div class="form-section">
                        <h3 class="section-title">
                            <i class="bi bi-pen"></i>
                            Signature & Date
                        </h3>
                        <div class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Signature</label>
                                <input type="text" class="form-control" name="signature" placeholder="Signatory name">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Date</label>
                                <input type="date" class="form-control" name="date">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Place</label>
                                <input type="text" class="form-control" name="place" placeholder="Location">
                            </div>
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <div class="text-center mt-5">
                        <button type="submit" class="btn btn-submit btn-lg text-white">
                            <i class="bi bi-file-earmark-pdf-fill me-2"></i>
                            Generate MAWB
                        </button>
                    </div>
                </form>

                <!-- Hidden HAWB Button -->
                <div id="hawbButtonContainer" style="display: none; text-align: center; margin-top: 20px;">
                    <!-- The href will be updated with the selected AWB number -->
                    <a id="createHawbBtn" class="btn btn-secondary" href="#">Generate HAWB</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
   <script>
  document.addEventListener('DOMContentLoaded', function() {
    // Form validation and floating labels
    const form = document.getElementById('mawbForm');
    form.addEventListener('submit', function(event) {
      if (!form.checkValidity()) {
        event.preventDefault();
        event.stopPropagation();
      }
      form.classList.add('was-validated');
    });
    const inputs = document.querySelectorAll('.form-control, .form-select');
    inputs.forEach(input => {
      input.addEventListener('focus', () => {
        input.parentElement.classList.add('focused');
      });
      input.addEventListener('blur', () => {
        if (!input.value) {
          input.parentElement.classList.remove('focused');
        }
      });
    });

    // Populate AWB numbers based on selected airline prefix
    const airlineSelect = document.getElementById('airlineSelect');
    const awbNumberContainer = document.getElementById('awbNumberContainer');
    const awbNumberSelect = document.getElementById('awbNumberSelect');
    const hawbButtonContainer = document.getElementById('hawbButtonContainer');
    const createHawbBtn = document.getElementById('createHawbBtn');

    // AWB data is passed from the server as a JSON string using Handlebars
    const awbNumbers = JSON.parse('{{{json awbNumbers}}}');

    airlineSelect.addEventListener('change', function () {
      const selectedPrefix = this.value;
      awbNumberSelect.innerHTML = '<option value="" selected disabled>Select AWB Number</option>';
      if (selectedPrefix) {
        awbNumberContainer.style.display = 'block';
        // Filter and populate AWB numbers based on the selected airline prefix
        awbNumbers.forEach(awb => {
          if (awb.prefix === selectedPrefix) {
            const option = document.createElement('option');
            option.value = awb.awbNumber;
            option.textContent = awb.awbNumber;
            awbNumberSelect.appendChild(option);
          }
        });
      } else {
        awbNumberContainer.style.display = 'none';
      }
      // Hide the HAWB button until an AWB number is selected
      hawbButtonContainer.style.display = 'none';
    });

    // When an AWB number is selected, update the HAWB link and reveal the hidden button
    awbNumberSelect.addEventListener('change', function () {
      const selectedAwb = this.value;
      if (selectedAwb) {
        // Update the HAWB link with the selected MAWB (AWB) number as a query parameter.
        createHawbBtn.href = "/admin/hawb?mawbNumber=" + encodeURIComponent(selectedAwb);
        hawbButtonContainer.style.display = 'block';
      } else {
        hawbButtonContainer.style.display = 'none';
      }
    });
  });

  // Add this to your existing JavaScript in the document
// Add these to your existing JavaScript

// Add visual feedback for form interactions
document.querySelectorAll('.form-control, .form-select').forEach(input => {
  // Add "floating" effect on focus
  input.addEventListener('focus', () => {
    input.style.transform = 'translateY(-2px)';
  });
  
  input.addEventListener('blur', () => {
    input.style.transform = '';
  });
  
  // Add validation styling on change
  input.addEventListener('change', () => {
    if (input.value) {
      input.classList.add('is-valid');
    } else {
      input.classList.remove('is-valid');
    }
  });
});

// Improve cargo item functionality
function createCargoItem(count) {
  const newCargoRow = document.createElement('div');
  newCargoRow.className = 'cargo-item';
  newCargoRow.dataset.cargoId = count;
  
  newCargoRow.innerHTML = `
    <div class="cargo-title">Cargo Item #${count}</div>
    <div class="row g-4">
      <div class="col-md-4">
        <label class="form-label">Gross Weight</label>
        <input type="text" class="form-control" name="grossWeight_${count}" placeholder="Enter weight">
      </div>
      <div class="col-md-4">
        <label class="form-label">Nature and Quantity</label>
        <input type="text" class="form-control" name="consolidated_${count}" placeholder="Description">
      </div>
      <div class="col-md-4">
        <label class="form-label">Dimensions</label>
        <input type="text" class="form-control" name="dimensions_${count}" placeholder="LxWxH">
      </div>
      <div class="col-md-3 mt-3">
        <label class="form-label">QTY</label>
        <input type="text" class="form-control" name="QTY_${count}" placeholder="Quantity">
      </div>
      <div class="col-md-3 mt-3">
        <label class="form-label">Rate Charge</label>
        <input type="text" class="form-control" name="RATECHARGE_${count}" placeholder="Rate">
      </div>
      <div class="col-md-6 mt-4 text-end">
        <button type="button" class="btn btn-danger remove-cargo-btn" data-cargo-id="${count}">
          <i class="bi bi-trash"></i> Remove Item
        </button>
      </div>
    </div>
  `;
  
  return newCargoRow;
}

// Replace your current cargo item creation with this enhanced version
document.addEventListener('DOMContentLoaded', function() {
  const addCargoBtn = document.createElement('button');
  addCargoBtn.type = 'button';
  addCargoBtn.className = 'btn btn-primary mt-4';
  addCargoBtn.innerHTML = '<i class="bi bi-plus-circle"></i> Add Cargo Item';
  
  const cargoSection = document.querySelector('.form-section:nth-of-type(4)');
  cargoSection.appendChild(addCargoBtn);
  
  let cargoCount = 1;
  
  addCargoBtn.addEventListener('click', function() {
    cargoCount++;
    const newCargoRow = createCargoItem(cargoCount);
    cargoSection.insertBefore(newCargoRow, addCargoBtn);
    
    newCargoRow.querySelector('.remove-cargo-btn').addEventListener('click', function() {
      const cargoId = this.dataset.cargoId;
      document.querySelector(`.cargo-item[data-cargo-id="${cargoId}"]`).remove();
      updateCargoNumbering();
    });
  });
});
    
  
  
  // Function to update cargo item numbering
  function updateCargoNumbering() {
    const cargoItems = document.querySelectorAll('.cargo-item');
    cargoItems.forEach((item, index) => {
      const cargoNum = index + 2; // Start from 2 since the first one is the default
      item.dataset.cargoId = cargoNum;
      item.querySelector('.cargo-title').textContent = `Cargo Item #${cargoNum}`;
      item.querySelector('.remove-cargo-btn').dataset.cargoId = cargoNum;
    });
  }

</script>
</body>
</html>