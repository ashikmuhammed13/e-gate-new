<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern MAWB Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #0ea5e9;
            --secondary-color: #0f172a;
            --accent-color: #38bdf8;
            --border-color: #e2e8f0;
            --hover-color: #0284c7;
            --bg-color: #f8fafc;
            --shadow-color: rgba(0, 0, 0, 0.04);
            --text-color: #334155;
            --section-bg: #ffffff;
            
    --primary-gradient: linear-gradient(135deg, #4361ee 0%, #4895ef 100%);
    --glass-bg: rgba(255, 255, 255, 0.95);
    --glass-shadow: 0 8px 32px rgba(31, 38, 135, 0.15);
    --card-border: 1px solid rgba(255, 255, 255, 0.18);
    --neon-glow: 0 0 15px rgba(67, 97, 238, 0.3);
    --sidebar-width: 280px;
    --sidebar-bg: rgba(15, 23, 42, 0.95);
    --sidebar-hover: rgba(30, 41, 59, 0.8);
    --neon-accent: #0ea5e9;
    --glass-effect: blur(10px);
}
        /* Sidebar Base Styles */
.sidebar {
    width: var(--sidebar-width);
    background: var(--sidebar-bg);
    backdrop-filter: var(--glass-effect);
    -webkit-backdrop-filter: var(--glass-effect);
    border-right: 1px solid rgba(255, 255, 255, 0.1);
    position: fixed;
    height: 100vh;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    overflow-y: auto;
    scrollbar-width: none;
    z-index: 1000;
}

.sidebar::-webkit-scrollbar {
    display: none;
}

/* Logo Section */
.sidebar .logo {
    padding: 2rem 1.5rem;
    background: linear-gradient(
        to bottom,
        rgba(15, 23, 42, 0.98),
        rgba(15, 23, 42, 0.95)
    );
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.sidebar .logo h4 {
    font-size: 1.25rem;
    font-weight: 600;
    background: linear-gradient(120deg, #fff, var(--neon-accent));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    letter-spacing: 0.5px;
}

/* Navigation Links */
.sidebar .nav-link {
    color: rgba(255, 255, 255, 0.7);
    padding: 1rem 1.5rem;
    margin: 0.3rem 0.8rem;
    border-radius: 12px;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    position: relative;
    text-decoration: none;
}

.sidebar .nav-link:hover {
    background: var(--sidebar-hover);
    color: var(--neon-accent);
    box-shadow: var(--neon-glow);
}

.sidebar .nav-link i {
    margin-right: 1rem;
    font-size: 1.1rem;
    transition: all 0.3s ease;
}

.sidebar .nav-link.active-nav-link {
    background: rgba(14, 165, 233, 0.15);
    color: var(--neon-accent);
    box-shadow: var(--neon-glow);
}

.sidebar .nav-link.active-nav-link::before {
    content: '';
    position: absolute;
    left: -0.8rem;
    top: 50%;
    transform: translateY(-50%);
    width: 4px;
    height: 20px;
    background: var(--neon-accent);
    border-radius: 0 4px 4px 0;
    box-shadow: var(--neon-glow);
}

.nav.flex-column {
    padding: 1rem 0;
}
        

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            
    background: linear-gradient(135deg, #f6f7ff 0%, #eef1ff 100%);
    min-height: 100vh;
    margin: 0;
    padding: 0;
        }

        .main-container {
            min-height: 100vh;
            padding: 2rem 0;
margin-left: var(--sidebar-width);
    padding: 2rem;
    min-height: 100vh;
    background: linear-gradient(135deg, #f6f7ff 0%, #eef1ff 100%);
    transition: margin-left 0.4s ease;
        }
     

        .header {
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            padding: 3rem 2rem;
            border-radius: 20px;
            margin-bottom: 2rem;
            box-shadow: 0 15px 30px rgba(14, 165, 233, 0.15);
        }

        .header h1 {
            font-weight: 700;
            font-size: 2.75rem;
            margin-bottom: 1rem;
            color: white;
        }

        .header p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1rem;
        }

        .form-section {
            background: var(--section-bg);
            border-radius: 20px;
            box-shadow: 0 8px 25px var(--shadow-color);
            padding: 2.5rem;
            margin-bottom: 2rem;
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
        }

        .form-section:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--secondary-color);
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--border-color);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .section-title i {
            color: var(--primary-color);
        }

        .form-label {
            font-weight: 500;
            color: var(--secondary-color);
            margin-bottom: 0.75rem;
            font-size: 0.95rem;
        }

        .form-control, .form-select {
            border: 1.5px solid var(--border-color);
            padding: 0.875rem 1rem;
            border-radius: 10px;
            font-size: 0.95rem;
            transition: all 0.2s ease;
            background-color: #ffffff;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.15);
        }

        .search-input {
            background-color: #f1f5f9;
            border: 1.5px solid var(--border-color);
            padding: 0.875rem 1rem;
            border-radius: 10px;
            margin-bottom: 1rem;
        }

        .search-input::placeholder {
            color: #94a3b8;
        }

        .btn-submit {
            background: var(--primary-color);
            border: none;
            padding: 1.25rem 3rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-size: 1.1rem;
            letter-spacing: 0.5px;
        }

        .btn-submit:hover {
            background: var(--hover-color);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(14, 165, 233, 0.25);
        }

        .form-check {
            margin-top: 0.75rem;
        }

        .form-check-input {
            width: 1.2em;
            height: 1.2em;
            margin-top: 0.25em;
            border: 1.5px solid var(--border-color);
        }

        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .form-check-label {
            font-size: 0.95rem;
            color: var(--text-color);
        }

        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }

        .required-field::after {
            content: "*";
            color: #ef4444;
            margin-left: 4px;
        }

        .input-group-text {
            background-color: #f1f5f9;
            border: 1.5px solid var(--border-color);
            border-radius: 10px;
            padding: 0.875rem 1rem;
        }

        .info-tooltip {
            color: var(--primary-color);
            cursor: pointer;
            margin-left: 0.5rem;
        }

        .custom-select {
            appearance: none;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3e%3cpath fill='none' stroke='%23343a40' stroke-linecap='round' stroke-linejoin='round' stroke-width='2' d='M2 5l6 6 6-6'/%3e%3c/svg%3e");
            background-repeat: no-repeat;
            background-position: right 1rem center;
            background-size: 16px 12px;
            padding-right: 2.5rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header {
                padding: 2rem 1.5rem;
            }

            .header h1 {
                font-size: 2rem;
            }

            .form-section {
                padding: 1.5rem;
            }

            .btn-submit {
                width: 100%;
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
     <div class="wrapper">
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="logo">
                <h4 class="mb-0">E Gate Shipping</h4>
            </div>
            <div class="nav flex-column">
                <a href="/admin/track"class="nav-link ">
                    <i class="bi bi-truck"></i> 
                    Tracking
                </a>

               <a href="/admin/createAwb"class="nav-link">
                    <i class="bi bi-files"></i>
                    Create AWB
                </a>
                 <a href="/admin/hawb" class="nav-link ">
                    <i class="bi bi-file-text"></i> 
                    House AWB
                </a>
                <a href="/admin/mawb" class="nav-link active-nav-link">
                    <i class="bi bi-file-earmark-break"></i> 
                    Master AWB
                </a>

                <a href="/admin/fetchShipment" class="nav-link">
                    <i class="bi bi-pencil-square"></i>
                    Update AWB
                </a>
               <a href="/admin/getShipment" class="nav-link ">
                    <i class="bi bi-boxes"></i>
                    All Shipments
                </a>
                <a href="/admin/profile"class="nav-link">
                    <i class="bi bi-gear"></i>
                    Admin Profile
                </a>
            </div>
        </nav>
    <div class="main-container">
        <div class="container">
            <div class="header text-center">
                <h1>Master Air Waybill</h1>
                <p class="mb-0">Digital Air Cargo Management System</p>
            </div>

           <form id="mawbForm" action="/admin/generate-mawb" method="POST">
                <!-- AWB Details -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-file-earmark-text"></i>
                        AWB Details
                    </h3>
                    <div class="row g-4">
                  <div class="col-md-4">
    <label class="form-label">Select Airline</label>
    <select class="form-select" id="airlineSelect" name="airline">
        <option value="" selected disabled>Select Airline</option>
        {{#each airlines}}
            <option value="{{prefix}}">{{airlineName}}</option>
        {{/each}}
    </select>
</div>

<div class="col-md-4" id="awbNumberContainer" style="display:none;">
    <label class="form-label">AWB Number</label>
    <select class="form-select" id="awbNumberSelect" name="awbNumber">
        <option value="" selected disabled>Select AWB Number</option>
        <!-- AWB numbers will be dynamically populated -->
    </select>
</div>


                        <div class="col-md-4">
                            <label class="form-label">IATA Code</label>
                            <input type="text" class="form-control" name="iataCode">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Carrier Code</label>
                            <input type="text" class="form-control" name="carrierCode">
                        </div>
                    </div>
                </div>

                <!-- Shipper & Consignee -->
               <!-- Shipper & Consignee -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-people-fill"></i>
                        Shipper & Consignee Information
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <input type="text" class="search-input" placeholder="Search shipper in address book">
                            </div>
                            <label class="form-label required-field">Shipper Details</label>
                            <textarea class="form-control"  name="shipperName"required 
                                    placeholder="Enter complete shipper information"></textarea>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="saveShipperAddress">
                                <label class="form-check-label" for="saveShipperAddress">
                                    Save to address book
                                </label>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <input type="text" class="search-input" placeholder="Search consignee in address book">
                            </div>
                            <label class="form-label required-field">Consignee Details</label>
                            <textarea class="form-control" name="consigneeName" required 
                                    placeholder="Enter complete consignee information"></textarea>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="saveConsigneeAddress">
                                <label class="form-check-label" for="saveConsigneeAddress">
                                    Save to address book
                                </label>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Routing Information -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-geo-alt-fill"></i>
                        Routing Information
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-3">
                            <label class="form-label">Departure</label>
                            <input type="text" class="form-control" name="departure">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Destination</label>
                            <input type="text" class="form-control" name="destination">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Routing TO1</label>
                            <input type="text" class="form-control" name="routingTO1">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">ICN</label>
                            <input type="text" class="form-control" name="ICN">
                        </div>
                    </div>
                </div>

                <!-- Cargo Details -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-box-seam"></i>
                        Cargo Details
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-4">
                            <label class="form-label">Gross Weight</label>
                            <input type="text" class="form-control" name="grossWeight">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Kilo Gram</label>
                            <input type="text" class="form-control" name="kiloGram">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Dimensions</label>
                            <input type="text" class="form-control" name="dimensions">
                        </div>
                        <div class="col-12">
                            <label class="form-label">Handling Information</label>
                            <textarea class="form-control" name="handlingInformation" rows="3"></textarea>
                        </div>
                    </div>
                </div>

                <!-- Charges -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-currency-dollar"></i>
                        Charges & Payment
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-3">
                            <label class="form-label">Prepaid</label>
                            <input type="text" class="form-control" name="prepaid">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Rate Charge</label>
                            <input type="text" class="form-control" name="RATECHARGE">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Total Charges Carrier</label>
                            <input type="text" class="form-control" name="totalChargesCarrier">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Freight</label>
                            <input type="text" class="form-control" name="frieght">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Total QTY</label>
                            <input type="text" class="form-control" name="totalQTY">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">QTY</label>
                            <input type="text" class="form-control" name="QTY">
                        </div>
                    </div>
                </div>

                <!-- Additional Information -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-info-circle"></i>
                        Additional Information
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-4">
                            <label class="form-label">SAR</label>
                            <input type="text" class="form-control" name="SAR">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">PP</label>
                            <input type="text" class="form-control" name="PP">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Consolidated</label>
                            <input type="text" class="form-control" name="consolidated">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">COLUMN1</label>
                            <input type="text" class="form-control" name="COLUMN1">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">COLUMN2</label>
                            <input type="text" class="form-control" name="COLUMN2">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">NVD</label>
                            <input type="text" class="form-control" name="NVD">
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">NCV</label>
                            <input type="text" class="form-control" name="NCV">
                        </div>
                    </div>
                </div>

                <!-- Signature Section -->
                <div class="form-section">
                    <h3 class="section-title">
                        <i class="bi bi-pen"></i>
                        Signature & Date
                    </h3>
                    <div class="row g-4">
                        <div class="col-md-4">
                            <label class="form-label">Signature</label>
                            <input type="text" class="form-control" name="signature">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Date</label>
                            <input type="date" class="form-control" name="date">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label">Place</label>
                            <input type="text" class="form-control" name="place">
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <div class="text-center mt-4">
                    <button type="submit" class="btn btn-submit btn-lg text-white">
                        <i class="bi bi-file-earmark-pdf-fill me-2"></i>
                        Generate MAWB
                    </button>
                </div>
            </form>
        </div>
    </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Form validation and interaction enhancements
        document.addEventListener('DOMContentLoaded', function() {
            // Form validation
            const form = document.getElementById('mawbForm');
            
            form.addEventListener('submit', function(event) {
                if (!form.checkValidity()) {
                    event.preventDefault();
                    event.stopPropagation();
                }
                form.classList.add('was-validated');
            });

            // Add floating labels behavior
            const inputs = document.querySelectorAll('.form-control, .form-select');
            inputs.forEach(input => {
                input.addEventListener('focus', () => {
                    input.parentElement.classList.add('focused');
                });
                input.addEventListener('blur', () => {
                    if (!input.value) {
                        input.parentElement.classList.remove('focused');
                    }
                });
            });
        });

          document.addEventListener('DOMContentLoaded', function () {
        const airlineSelect = document.getElementById('airlineSelect');
        const awbNumberContainer = document.getElementById('awbNumberContainer');
        const awbNumberSelect = document.getElementById('awbNumberSelect');

        // Fetch AWB data from the server (data is embedded in Handlebars)
        const awbNumbers = JSON.parse('{{{json awbNumbers}}}'); 

        airlineSelect.addEventListener('change', function () {
            const selectedPrefix = this.value;

            // Clear previous AWB numbers
            awbNumberSelect.innerHTML = '<option value="" selected disabled>Select AWB Number</option>';

            if (selectedPrefix) {
                // Show AWB container
                awbNumberContainer.style.display = 'block';

                // Filter and populate AWB numbers based on selected airline prefix
                awbNumbers.forEach(awb => {
                    if (awb.prefix === selectedPrefix) {
                        const option = document.createElement('option');
                        option.value = awb.awbNumber;
                        option.textContent = awb.awbNumber;
                        awbNumberSelect.appendChild(option);
                    }
                });
            } else {
                awbNumberContainer.style.display = 'none';
            }
        });
    });
    </script>
</body>
</html>